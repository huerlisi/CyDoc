.contextual
  = link_to "Liste", invoice_batch_jobs_path
  = link_to "Erneut drucken", reprint_invoice_batch_job_path, :method => :post
= boot_page_title
%h3= t_attr :failed_jobs
= failed_jobs(resource)
%h3 Rechnungen
= render resource.invoices.page(0).per(resource.invoices.count)
