.contextual
  = icon_link_to :index, invoice_batch_jobs_path
  = icon_link_to "Erneut drucken", reprint_invoice_batch_job_path, :method => :post

= boot_page_title

%h3= t_attr :failed_jobs
= failed_jobs(resource)

%h3= t_attr(:bills)
= render :partial => 'invoices/list', :collection => resource.invoices
