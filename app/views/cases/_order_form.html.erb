<fieldset>
  <legend>Auftragsformular</legend>
  <%= link_to(image_tag(url_for(:action => :order_form, :id => params[:id]), :style => 'width: 100%'), url_for(:action => :order_form, :id => params[:id])) %>
</fieldset>
