<div class="contextual">
    <%= link_to "Ändern", edit_doctor_path(@doctor), :class => "icon icon-edit" %>
</div>

<h3>Drucker</h3>

<% @doctor.office && printers = @doctor.office.printers %>
<%- if printers %>
  <div class="box">
    Druckserver: <%= printers[:cups_host] %>
    <table>
      <tr>
        <th>Zweck</th>
        <th>Schacht</th>
      </tr>
      <% printers[:trays].each do |ident, value| %>
      <tr>
        <th><%= ident %></th>
        <td><%= value %></td>
      </tr>
      <% end %>
    </table>
  </div>
<%- end %>
