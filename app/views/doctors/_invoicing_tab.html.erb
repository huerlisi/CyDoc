<div class="contextual">
  <%= link_to "Ändern", edit_doctor_path(@doctor), :class => "icon icon-edit" %>
</div>

<h3>VESR Einstellungen</h3>

<div class="box">
  <table>
    <tr>
      <th><%= t_attr :print_payment_for %>:</th>
      <td><%= t(@doctor.print_payment_for, :scope => :boolean) %></td>
    </tr>
    <% if @doctor.esr_account %>
      <tr>
        <th>Bank:</th>
        <td><%= @doctor.esr_account.bank %></td>
      </tr>
      <tr>
        <th>ESR:</th>
        <td><%= @doctor.esr_account.esr_id %></td>
      </tr>
      <tr>
        <th>Postcheck Konto:</th>
        <td><%= @doctor.esr_account.pc_id %></td>
      </tr>
    <% end %>
  </table>
</div>
