<% fields_for :account, :url => {:action => 'create'} do |f| %>
  <tr>
    <th>Kontonummer:</th>
    <td><%= f.text_field :number %></td>
  </tr>
  <tr>
    <th>ESR:</th>
    <td><%= f.text_field :esr_id %></td>
  </tr>
  <tr>
    <th>Postcheck Konto:</th>
    <td><%= f.text_field :pc_id %></td>
  </tr>
<% end %>
