<%= error_messages_for :invoice %>

<% form_for :invoice, :url => {:action => 'create'} do |f| %>
  <p>
    Datum: <%= f.text_field :date %>

    <%= f.submit "Generieren" %>

    <%= hidden_field 'tiers', :patient_id %>
  </p>
<% end %>
