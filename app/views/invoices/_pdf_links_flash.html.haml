#notice_flash.flash.notice
  %p
    = button_to_function t_action(:close), "Element.hide('notice_flash')", :style => 'float: right'
    = t('printing.pdf_done')
    - views.each do |view|
      = icon_link_to view, polymorphic_url([view, @invoice], :format => :pdf), :icon => 'pdf', :target => view
