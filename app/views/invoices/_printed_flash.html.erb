<div id="invoice_flash" class="flash notice">
  <p>Rechnung an Drucker gesendet...</p>
  <% unless params[:print_copy] %>
  <p>
    Möchten Sie die Rechnung verbuchen?
    <%= button_to_function 'Ja', remote_function(:url => book_invoice_url(@invoice)) %>
    <%= button_to_function 'Nein', "Element.hide('invoice_flash')" %>
  </p>
  <% end %>
</div>
