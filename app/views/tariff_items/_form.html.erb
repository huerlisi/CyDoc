<script type="text/javascript">
  $('record_tarmed_code').focus()
</script>

<%= error_messages_for :record_tarmed %>

<% form_for :record_tarmed, :url => {:controller => 'tariff_items', :action => 'create'} do |f| %>
Tarmed Leistung:&nbsp;<%= text_field_with_auto_complete :record_tarmed, :code, {}, {:url => {:controller => 'tariff_items', :action => "auto_complete_for_record_tarmed_code"}, :min_chars => 2} %>
Datum:&nbsp;<%= f.text_field :date, :size => 9 %>
<%= f.submit "Erfassen" %>

<%= f.hidden_field :patient_id %>
<%= f.hidden_field :provider_id %>
<%= f.hidden_field :responsible_id %>
<%= f.hidden_field :quantity %>
<% end %>
