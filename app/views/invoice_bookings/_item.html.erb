<tr>
  <td style="width: 10em"><%=l item.value_date %></td>
  <td>
    <%= link_to item.title, item %>
    <%= '<hr style="height: 1px; margin: 0"/>' unless item.comments.blank? %>
    <%=h item.comments %>
  </td>
  <td style="text-align: right"><%=h sprintf("%0.2f", item.accounted_amount(Invoice::DEBIT_ACCOUNT)) %>&nbsp;CHF</td>
</tr>
